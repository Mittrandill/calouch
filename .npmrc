# React Native / Metro sembolik linkli node_modules ağacını güvenilir çözemez.
# Expo monorepo kılavuzunun gereği: hoisted linker.
node-linker=hoisted

# Sürümler açıkça yönetilir; sessiz peer düzeltmesi istemiyoruz.
auto-install-peers=false
strict-peer-dependencies=false
